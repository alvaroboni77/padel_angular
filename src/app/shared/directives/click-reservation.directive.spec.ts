import { ClickReservationDirective } from './click-reservation.directive';

describe('ClickReservationDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickReservationDirective();
    expect(directive).toBeTruthy();
  });
});
